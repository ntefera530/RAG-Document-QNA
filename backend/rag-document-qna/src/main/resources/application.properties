spring.application.name=rag-document-qna

# Server
server.port=8080

# PostgreSQL - overridden by Docker environment variables
spring.datasource.url=jdbc:postgresql://localhost:5432/ragdb
spring.datasource.username=raguser
spring.datasource.password=ragpassword
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# Ollama - overridden by Docker environment variables
spring.ai.ollama.base-url=http://localhost:11434
spring.ai.ollama.chat.model=llama3.1:8b
spring.ai.ollama.embedding.model=nomic-embed-text

# PGVector
spring.ai.vectorstore.pgvector.initialize-schema=true
spring.ai.vectorstore.pgvector.dimensions=768
spring.ai.vectorstore.pgvector.distance-type=COSINE_DISTANCE
